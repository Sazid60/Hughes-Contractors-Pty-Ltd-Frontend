export interface IProject {
    _id: string,
    projectImage: string;
    title: string;
    client: string;
    location: string;
    projectYear: string;
    duration: string;
    description: string;
}
